name: Research CI

on:
  push:
    paths:
      - 'research/**'
  pull_request:
    paths:
      - 'research/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo "Compiling LaTeX..."
