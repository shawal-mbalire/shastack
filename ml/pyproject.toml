[project]
name = "shastack-ml"
version = "0.1.0"
description = "ML module for shastack"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "torchvision",
]

[tool.uv]
managed = true
