# Landing Page

Public-facing landing page for shastack, built with Angular.

## Development

```bash
# Install dependencies
npm install

# Start dev server
ng serve

# Build for production
ng build --configuration production
```

## Deployment

The landing page is automatically deployed to Firebase Hosting on merge to `main`.

See `.github/workflows/firebase-hosting-merge.yml` for deployment configuration.
